import AccountPanel from "@/components/AccountPanel";

export const metadata = { title: "Sign in | The Second Round" };

export default function SignInPage() {
  return <AccountPanel initialMode="signin" />;
}
