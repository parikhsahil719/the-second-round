import AccountPanel from "@/components/AccountPanel";

export const metadata = { title: "Your account | The Second Round" };

export default function AccountPage() {
  return <AccountPanel initialMode="signin" />;
}
